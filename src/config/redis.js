// const { createClient } = require("redis");

// const redisClient = createClient(6079);

// redisClient.on("error", (error) => console.log("Redis Client Error", error));

// redisClient.connect();

// module.exports = redisClient;